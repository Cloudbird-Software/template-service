// W5-C1 脏 PR fixture：浅模块的实现侧（.github#224 AC-1 第二类配套）
// 拷入 fixture 仓时去掉 .txt 后缀置于 src/shallow/impl.ts。
export function implOne(x) {
  return x + 1;
}

export function implTwo(x) {
  return x + 2;
}

export function implThree(x) {
  return x + 3;
}
