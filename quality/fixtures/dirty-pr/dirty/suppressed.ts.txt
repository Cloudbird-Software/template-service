// W5-C1 脏 PR fixture：抑制标记样本（.github#224 AC-1 第四类）
// 拷入 fixture 仓时去掉 .txt 后缀、__ESLINT_MARK__ 由测试脚本拼回真标记置于
// src/suppressed.ts——不带 ADR/issue 引用，g040 cx-suppression-growth 必拦
// （带引用才走豁免通道）。拆形占位：org 抑制预算对全树计 \b<标记>\b（ADR-0036），
// fixture 模板不自计入。
// __ESLINT_MARK__-next-line no-unused-vars
function legacyCalc(x) {
  const unused = 1;
  return x * 2 + unused;
}

void legacyCalc;
